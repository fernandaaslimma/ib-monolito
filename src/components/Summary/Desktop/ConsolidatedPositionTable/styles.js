import styled from "styled-components";
import { rem } from "../../../../styles/tools";

export const RedirectBox = styled.div`
  position: relative;
  padding: ${rem(10)} 0 ${rem(18)} ${rem(50)};
`;
