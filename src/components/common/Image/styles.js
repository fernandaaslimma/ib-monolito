import styled from "styled-components";
import { rem } from "../../../styles/tools";

export const WapperImg = styled.img`
  height: ${rem(36)};
  width: ${rem(36)};
  border-radius: ${rem(6)};
  border: 1px solid #d9e0e4;
`;
