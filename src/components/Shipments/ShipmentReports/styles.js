import styled from "styled-components";
import { rem } from "../../../styles/tools";

export const Container = styled.div`
  max-width: ${rem(1152)};
  margin: 0 auto;
  padding-top: ${rem(24)};
`;
