import styled from "styled-components";
import { rem } from "../../../../styles/tools";

export const Content = styled.div`
  max-width: ${rem(1200)};
  margin: ${rem(56)} auto auto auto;
  padding: 0 ${rem(21.5)} 0 ${rem(21.5)};
`;
