variables:
  collection: it
  projeto: internetbankingmfe
  repositorio: ib-monolito
  organization: 'BocomBBM'
  collection.min: 'it'
  project: $(System.TeamProject)
  project.min: $[lower(replace(variables['System.TeamProject'],' ',''))]
  repository: $(Build.Repository.Name)
  repository.min: "ib-monolito"
  buildnumber: $(Build.BuildNumber)
  FRONT_VERSION: $(Build.SourceBranch)
  isMaster: $[eq(variables['Build.SourceBranch'], 'refs/heads/master')]
  buildConfiguration: release
  acrEndpoint: "acr dev"
  projectKey: 'internetbankingfront'
